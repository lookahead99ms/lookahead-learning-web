/** Connected delivery: content is authorized by the same-origin API. */
export const environment = {
  production: true,
  protectedContent: true,
  apiBaseUrl: '/api',
  authorPreviewsBaseUrl: '/bff/author/previews/',
  accountPlansEnabled: true,
};
