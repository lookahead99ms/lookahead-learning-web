/** Standalone public demonstration: browser-local plans, no account API calls. */
export const environment = {
  production: false,
  protectedContent: false,
  apiBaseUrl: '/api',
  accountPlansEnabled: false,
};
